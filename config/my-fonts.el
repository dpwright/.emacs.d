(defun lotr ()
  (interactive)
  (set-default-font "Luminari 14"))

(defun mono ()
  (interactive)
  (set-default-font "Input Mono 12"))

(defun prop ()
  (interactive)
  (set-default-font "Georgia 14"))

(mono)

(provide 'my-fonts)
